<%= react_component('HelloWorld', greeting: 'Hello index') %>
<%= react_component('MyLists', my_lists: @lists) %>

<hr/>

<%= render @lists %>

<hr/>

<%= button_to 'New List', new_list_path, options = { method: :get } %>
