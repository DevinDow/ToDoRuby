<%= form_with(model: @task, url: @task.new_record? ? [@list, @task] : @task) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2>List = <%= @list.name %></h2>

  <table>
    <thead>
      <tr>
        <td><strong><%= form.label :done %></strong></td>
        <td><strong><%= form.label :priority %></strong></td>
        <td><strong><%= form.label :name, "Task" %></strong></td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong><%= form.check_box :done %></strong></td>
        <td><strong><%= form.number_field :priority, style: "width: 50px" %></strong></td>
        <td><strong><%= form.text_field :name %></strong></td>
        <td><%= form.submit %></td>
      </tr>
    </tbody>
  </table>

<% end %>
